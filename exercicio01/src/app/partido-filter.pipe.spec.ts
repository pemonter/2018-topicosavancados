import { PartidoFilterPipe } from './partido-filter.pipe';

describe('PartidoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PartidoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
