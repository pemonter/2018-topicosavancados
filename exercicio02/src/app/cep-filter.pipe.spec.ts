import { CepFilterPipe } from './cep-filter.pipe';

describe('CepFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CepFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
